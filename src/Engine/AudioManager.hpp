#ifndef AUDIO_HPP
#define AUDIO_HPP

/*alGetError();
ALCdevice* deviceAL = alcOpenDevice(NULL);
if (deviceAL == NULL) { LogError("Failed to init OpenAL device."); return;} 
ALCcontext* contextAL = alcCreateContext(deviceAL, NULL);
AL_CHECK(alcMakeContextCurrent(contextAL));*/

#endif
